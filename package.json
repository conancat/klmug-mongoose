{
  "name": "klmug-mongoose",
  "version": "0.0.0",
  "description": "Demonstration for MongoDB and NodeJS, with Mongoose",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "BSD",
  "dependencies": {
    "mongoose": ">= 0.0.1"
  }
}
